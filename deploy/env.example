# /etc/portfolio/env
# Copy this file to /etc/portfolio/env on the Pi and fill in real values.
# Permissions: sudo chown root:pi /etc/portfolio/env && sudo chmod 640 /etc/portfolio/env

DJANGO_SECRET_KEY=change-me-generate-with-python-secrets
DJANGO_DEBUG=false
DJANGO_ALLOWED_HOSTS=thvarzil.dev,www.thvarzil.dev

POSTGRES_DB=portfolio
POSTGRES_USER=portfolio
POSTGRES_PASSWORD=change-me
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
